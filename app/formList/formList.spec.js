
describe('myApp.formList module', function() {

  beforeEach(module('myApp.formList'));

  describe('formList controller', function(){

    it('should ....', inject(function($controller) {
      var formListCtrl = $controller('formListCtrl');
      expect(formListCtrl).toBeDefined();
    }));

  });
});